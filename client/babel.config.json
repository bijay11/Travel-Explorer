// this will add react transpilation process to webpack bundle process
{
  "presets": [
    // Transpile and Polyfill to ES5
    [
      "@babel/preset-env",
      {
        "useBuiltIns": "usage",
        "corejs": 3
      }
    ],
    "@babel/preset-react"
  ]
}
